{
  "compilerOptions": {
    "sourceMap": false, // 是否生成相应的Map映射的文件，默认：false。
    "target": "ES6", // 目标语言的版本
    "useDefineForClassFields": false, // 此标志用作迁移到即将推出的类字段标准版本的一部分，默认：false。
    "module": "esnext", // 指定编译后代码使用的模块化规范。
    "moduleResolution": "node", // 指定模块解析策略，node或classic
    "declaration": true, // 是否生成相应的 .d.ts 声明文件，默认：false。
    "allowJs": true, // 允许编译器编译JS，JSX文件
    "strict": true, // 开启所有严格的类型检查
    "noUnusedLocals": true, //是否检查未使用的局部变量
    "experimentalDecorators": true, //是否启用对装饰器的实验性支持，装饰器是一种语言特性，还没有完全被 JavaScript 规范批准
    "noEmitOnError": true, //有错误时不进行编译
    "resolveJsonModule": true, //是否解析 JSON 模块
    "esModuleInterop": true, // 是否通过为所有导入模块创建命名空间对象，允许CommonJS和ES模块之间的互操作性，开启改选项时，也自动开启allowSyntheticDefaultImports选项
    "removeComments": true, // 是否移除注释，默认：false
    "noEmit": false, // 不生成编译后的文件，默认：false。
    "skipLibCheck": true, // 是否跳过声明文件的类型检查，这可以在编译期间以牺牲类型系统准确性为代价来节省时间，默认：false。
    "jsx": "react", // 指定JSX代码生成用于的开发环境。
    "lib": ["esnext", "dom"], // 指定项目运行时使用的库。
    "plugins": [{ "name": "typescript-plugin-css-modules" }] // 在编辑器中运行的语言服务插件列表。
  }
}
